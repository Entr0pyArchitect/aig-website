import { RetroPanel } from "../components/retro/RetroPanel";

/*
  Account placeholder for later auth.
*/
export function Account() {
  return (
    <RetroPanel title="Account Portal">
      <div className="section-intro">
        <h1>Account system planned.</h1>
        <p>
          Add user accounts after the public pages, product catalog, BTC policy,
          and order storage are stable.
        </p>
      </div>
    </RetroPanel>
  );
}
